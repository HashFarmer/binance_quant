
client.get_account()

client.get_exchange_info()

把获取的数据推送到template文件里，即便推送到模板文件里的是一个很大的数据对象，也可以在模板里用模板引擎语言对之进行处理

能触发后端函数的其实是模板里的“表单”,“表单”包围起来的内容都是要提交到后端的，当然对应的后端函数也要做相应的对接

前端表单提交过来的内容全在 request.form 里
